<% content_for :title, "New matchup note" %>

<section class="mx-auto w-full max-w-3xl space-y-6">
  <div>
    <h1 class="text-2xl font-semibold text-white">New matchup note</h1>
    <p class="mt-1 text-sm text-zinc-400">Attach table memory to the deck, commander, opponent, pod, or session it came from.</p>
  </div>

  <%= render "form",
             matchup_note: @matchup_note,
             decks: @decks,
             commanders: @commanders,
             players: @players,
             pods: @pods,
             game_nights: @game_nights %>
</section>
